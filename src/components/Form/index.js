import React from 'react';

const Form = (props) => {
    return (
        <>
            <div style= {{ textAlign: 'center' }}>
                <p>Form</p>
            </div>
        </>
    );
};

export default Form;